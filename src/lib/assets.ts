/**
 * Brand lockups from public/sg-logos. Filenames carry spaces, so they are
 * URL-encoded here once instead of at every call site.
 *
 * Aspect ratio of every lockup: 1181 × 409.
 */
export const LOGO_ASPECT = 1181 / 409

/** Primary mark + dark wordmark — for white backgrounds. */
export const LOGO_MAIN = '/sg-logos/Logo%20SG%20main.svg'

/** Primary mark + white wordmark — for the primary band. */
export const LOGO_TEXT_WHITE = '/sg-logos/Logo%20SG%20text%20white.svg'

/** Fully white lockup. */
export const LOGO_WHITE = '/sg-logos/Logo%20SG%20white.svg'
